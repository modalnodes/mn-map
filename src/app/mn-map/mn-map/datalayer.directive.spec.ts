import { DatalayerDirective } from './datalayer.directive';

describe('DatalayerDirective', () => {
  it('should create an instance', () => {
    const directive = new DatalayerDirective();
    expect(directive).toBeTruthy();
  });
});
