import { MarkerlayerDirective } from './markerlayer.directive';

describe('MarkerlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerlayerDirective();
    expect(directive).toBeTruthy();
  });
});
