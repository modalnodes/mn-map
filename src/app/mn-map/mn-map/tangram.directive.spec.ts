import { TangramDirective } from './tangram.directive';

describe('TangramDirective', () => {
  it('should create an instance', () => {
    const directive = new TangramDirective();
    expect(directive).toBeTruthy();
  });
});
