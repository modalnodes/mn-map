import { NamedtilelayerDirective } from './namedtilelayer.directive';

describe('NamedtilelayerDirective', () => {
  it('should create an instance', () => {
    const directive = new NamedtilelayerDirective();
    expect(directive).toBeTruthy();
  });
});
